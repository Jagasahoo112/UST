import { TrailingTextPipe } from './trailing-text.pipe';

describe('TrailingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TrailingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
